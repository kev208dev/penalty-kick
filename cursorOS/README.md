# CursorOS

AI desktop theme generator MVP based on the CursorOS PRD.

## Setup

```bash
npm install
cp .env.example .env.local
npm run dev
```

Set `OPENROUTER_API_KEY` in `.env.local` to enable live generation.
